query getUrl($shortUrl: String!) {
    url(shortUrl: $shortUrl) {
        id
        url,
        shortUrl
    }
}

mutation createUrl($url: String!) {
    generateUrl(url: $url) {
        id
        url,
        shortUrl
    }
}
